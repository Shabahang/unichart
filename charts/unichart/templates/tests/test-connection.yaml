apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unichart.fullname" . }}-test-connection"
  labels:
    {{- include "unichart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unichart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
